<%= simple_form_for @charge, url: :gov_module_bplo_settings_charges do |f| %>
  <%= f.input :charge_scope, collection: Charge.charge_scopes.keys.to_a.map{|a| [a.titleize, a] } %>
  <%= f.input :name %>
  <%= f.input :amount %>
 
  <%= f.submit 'Create Charge' %>
<% end %>
